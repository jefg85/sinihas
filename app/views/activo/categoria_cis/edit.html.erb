<%= form_for(@activo_categoria_ci , :validate => true) do |f| %>
<div>
  <div class="content-box">
    <div class="content-header"><h3>
    <div class="title-box">
           Editar Categoría 
    </div>  
    <div class="btns-box">
    	<%= f.submit "Guardar" %>    
    	| <%= link_to 'Eliminar', @activo_categoria_ci, method: :delete, data: { confirm: 'Seguro de eliminar?' } %>         
    </div></h3></div>
    <div class="content">
     	<%= render :partial => "form", :locals => { :f => f } %>
    </div>
  </div>
</div>
<% end %>
